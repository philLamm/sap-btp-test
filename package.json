{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "10.5.1",
    "@sap/html5-repo-mock": "2.1.0"
  },
  "scripts": {
    "build-react-demo-test": "cd react-demo-test && if [ ! -d ${workspaceFolder}/react-frontend-test/react-demo-test/node_modules/react-scripts ] && [ ! -d ${workspaceFolder}/react-frontend-test/node_modules/react-scripts ]; then npm i; fi && npm run build"
  }
}
